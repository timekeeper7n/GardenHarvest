# 更新日志 (CHANGELOG)

## v2.0.1 (2026-09-05)

### 最终商店购买逻辑重做

- 告别固定坐标连招，改为实时识别商品按钮的四种状态并自动决策：
  - **最大強化**（粉）→ 可强化，优先点击
  - **投入**（灰）→ 矿石不够升一级也继续投入
  - **強化素材不足**（青）→ 矿石耗尽，立即点[完了]退出
  - **LvMAX**（橙）→ 已满级，跳过不浪费点击
- 一个子项买完自动切换下一个，全部满级自动结束；採掘栏无子项自动跳过
- 覆盖全部 6 个商品位（旧固定连招只点 5 个）
- 识别用"文字特写模板 + 多点匹配"（core 新增 `find_all_images`），
  不受按钮上价格数字变化影响
- 识别流程：強化素材不足为最高优先（矿石耗尽立刻退出），
  其次最大強化/投入，LvMAX 不处理

### 精简与体验

- 配置内置进 exe，**单个 exe 即可运行**，不再自动生成 config.json；
  GUI 新增 **[生成配置文件]** 按钮，需要自定义设置时按需导出
- 运行日志每轮开始自动清空，超过 500 行自动裁剪，长时间挂机不再无限增长

## v1.0.4 (2026-09-05)

- 修复商店连招"现场坐标校准"一直静默失效的问题：原参考图裁的是矿石
  计数器（数字一变就失配，实测只剩 0.43 分），换成项目1"タイプ装備"
  按钮的文字特写（文字固定不变）后校准恢复生效，商品点位映射误差 2~10px

## v1.0.3 (2026-09-05)

- 修复 help/助战界面被误认成"最终商店"的问题：两种界面的右下角按钮是
  同款白色圆角按钮（完了/立ち去る只差文字），原模板在 help 界面上分数
  高达 0.79、贴着 0.8 触发线波动，偶尔直接开跑最终商店购买连招导致混乱。
  现在"完了"触发模板改为文字特写，实测最终商店 0.999 / help 界面与
  道中商店 ≤0.50，彻底分离

## v1.0.2

- 修复小关卡结算界面角色头像（红发角色）被误认成"矿残留确认弹窗"：
  颜色识别增加锚点位置门，只接受弹窗按钮固定位置（±70 标准像素）附近
  的红色块，头像、路径地图红色节点等远处的红色元素一律不再误判

## v1.0.1

- 商店结算连招多轮循环漂移修复：执行期间锁定全局坐标校准 + 防跳变保护，
  并改为进店时用画面内的倍率/完了按钮现场计算缩放比例

## v1.0

- 首个公开版本：全流程截图识别自动挖矿（模式出击 → 配队 → 路径 → 战斗 →
  buff → 局内商店/支援 → 总结算 → 强化商店 → 退出循环）
